const scrapeProducts = async ({url}) => {
    if (!url) return "";
    try {
      console.log(url);
      // You can perform your scraping logic here
    } catch (error) {
      console.log(error);
    }
  };
  

  export default scrapeProducts;